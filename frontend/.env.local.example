# Privy (browser auth + embedded user wallet). Public.
NEXT_PUBLIC_PRIVY_APP_ID=

# trade-royal-backend base URLs (Railway in prod, localhost in dev).
NEXT_PUBLIC_API_URL=http://localhost:3000
# Optional explicit WS base; derived from API_URL if omitted (…/ws).
NEXT_PUBLIC_WS_URL=
