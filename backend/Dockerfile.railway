# Railway-specific Dockerfile (Node 24 — matches package.json engines).
FROM node:24-alpine

RUN npm install -g pnpm@10.14.0 --force

WORKDIR /app

COPY package.json pnpm-lock.yaml ./
RUN pnpm install --frozen-lockfile

COPY . .
RUN pnpm run build

EXPOSE 3000

CMD ["node", "dist/index.js"]
